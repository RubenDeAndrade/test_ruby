<h1 class="text-center">Associations</h1>

<table class="table">
  <thead>
    <tr><th>Nom</th></tr>
  </thead>
  <tbody>
    <% @associations.each do |association| %>
      <tr>
        <td><strong><%= association.name %></strong></td>
        <td><%= link_to 'Voir', association, class: 'btn btn-primary btn-sm' %></td>
        <td><%= link_to 'Éditer', edit_association_path(association), 
              class: 'btn btn-default btn-sm' %></td>
        <td><%= link_to 'Supprimer', association, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nouvelle Association', new_association_path %>
